# Dependencies (dev-only: the SQL test engine)
node_modules/

# Vercel's local link to the project (holds account/project ids)
.vercel/

# Generated by `npm run samples` / `npm test`; rebuilt on demand
samples/

# Local secrets and editor/OS noise
.env
.env.*
*.log
.DS_Store
Thumbs.db
